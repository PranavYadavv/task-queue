[project]
name = "task-queue"
version = "0.1.0"
description = "Lightweight async task queue with retry and dead letter support"
requires-python = ">=3.8"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"
